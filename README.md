# Service freebot

## Running
Приложение запускается в Docker контейнере
для запуска хватит:
docker-compose up --build

В переменныe окружения необходимо добавить переменные,
они указаны в .env.example 

Приложению необходим доступ во внешний интернет,
для оправки запросов в Telegram API
